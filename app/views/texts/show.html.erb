<div class="mx-auto text-left" style="width: 800px;">

  <%
    text =<<~TEXT
      # Macのショートカットキー一覧
    TEXT
  %>
  <%= markdown(text).html_safe %>

  <% @commands.each do |command| %>
    <tr>
      <td><%= command.title %></td>
      <td><%= command.content %></td><br>
    </tr>
  <% end %>
</div>
